# Database Configuration
# Set to "true" to use PostgreSQL, "false" to use SQLite (default for local development)
USE_POSTGRES=false

# PostgreSQL Connection (only used when USE_POSTGRES=true)
POSTGRES_DB=das_db
POSTGRES_USER=das_user
POSTGRES_PASSWORD=das_password
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Django Settings
SECRET_KEY=django-insecure-%!%%h$k34^5aae_rugfchoeph@_mj0=+=27%$2eynl7-8*^g!^
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# JWT Configuration
JWT_ACCESS_TOKEN_LIFETIME_DAYS=1
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# CORS Configuration
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOW_CREDENTIALS=True

# Ollama Configuration
OLLAMA_HOST=http://ollama:11434
